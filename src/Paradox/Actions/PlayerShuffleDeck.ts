import { Game } from "../Game";
import { Player } from "../Player";
import { GameState } from "../GameState";
import { Deck } from "../Deck";
import { GameEvents } from "../Events/GameEvents";

namespace Actions {
    export function shuffleDeck(gameState: GameState, deck: Deck) {
        GameEvents
    }
}